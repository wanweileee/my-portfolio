export const Chatbot = () => {
    return (
      <div style={{ padding: '40px' }}>
        <h2>Business Startup</h2>
        <p>This project focuses on ...</p>
        {/* Add images, features, tech stack, etc. */}
      </div>
    );
  };